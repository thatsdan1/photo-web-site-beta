{
    "version": 2,
    "functions": {
      "api/booking": {
        "maxDuration": 10
      }
    },
    "routes": [
      { "src": "/api/(.*)", "dest": "/server.js" },
      { "src": "/(.*)", "dest": "/public/$1" }
    ]
  }
  
  
  
  